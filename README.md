# Automation

Cloud-first browser automation platform that learns web workflows from demonstrations, combines deterministic browser control with model reasoning, and executes scheduled automations even when the user's device is offline.

Development is being built incrementally with production-oriented architecture, explicit failure recovery, and provider-pluggable credentials.
